[workspace]
members = ["contracts/component-lock", "contracts/always-success"]
exclude = ["tests"]

[profile.release]
overflow-checks = true
strip = true
opt-level = 's'
lto = false
codegen-units = 1
